{#
/**
 * @file
 * Pluraltheme's theme implementation to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template normally located in the
 * core/modules/system directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 * - logo: The url of the logo image, as defined in theme settings.
 * - site_name: The name of the site. This is empty when displaying the site
 *   name has been disabled in the theme settings.
 * - site_slogan: The slogan of the site. This is empty when displaying the site
 *   slogan has been disabled in theme settings.
 * - hide_site_name: A flag indicating if the site name has been toggled off on
 *   the theme settings page. If hidden, the "visually-hidden" class is added
 *   to make the site name visually hidden, but still accessible.
 * - hide_site_slogan: A flag indicating if the site slogan has been toggled off
 *   on the theme settings page. If hidden, the "visually-hidden" class is
 *   added to make the site slogan visually hidden, but still accessible.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - tabs: Tabs linking to any sub-pages beneath the current page (e.g., the
 *   view and edit tabs when displaying a node).
 * - action_links: Actions local to the page, such as "Add menu" on the menu
 *   administration interface.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.sidebar_left: Items for the Left sidebar region.
 * - page.sidebar_right: Items for the Right sidebar region.
 * - page.content: Items for the Content region.
 * - page.header: Items for the Header region.
 * - page.footer: Items for the Footer region.
 * - page.primary: Items for the Primary Links region.
 * - page.secondary: Items for the Secondary Links region.
 * - page.help: Dynamic help text, mostly for admin pages.
 * - page.content_top: Items for the Content Top region.
 * - page.content_bottom: Items for the Content Bottom region.
 * - page.breadcrumb: Items for the Breadcrumb region.
 * - page.user1: Items for the User 1 region.
 * - page.user2: Items for the User 2 region.
 * - page.user3: Items for the User 3 region.
 * - page.user4: Items for the User 4 region.
 * - page.user5: Items for the User 5 region.
 * - page.user6: Items for the User 6 region.
 * - page.top_banner: Items for the Top Banner 468 x 60 region.
 * - page.phone: Items for the phone region.
 * - page.logo: Items for the plogo region.
 *
 * Custom flags:
 * - layout_style: The style to be used for layout (Portal | Blog | News).
 * - width_style: The width style (Fixed | Fluid).
 * - fixedwidth: Fixed with size (only applies for fixed width).
 * - leftwidth: The width of the left region.
 * - rightwidth: The width of the right region.
 * - color: The theme color (Default | Blue | Green).
 * - show_breadcrumb: Whether to show breadcrumbs or not.
 * - use_default_banner: Whether to use the default banner or not.
 *
 * @see template_preprocess_page()
 * @see pluraltheme_preprocess_page()
 * @see html.html.twig
 */
#}
<!-- Primary and Secondary Links -->
{% if page.primary %}
  <div id="primary-links" class="clear-block">
    {{ page.primary }}
  </div>
{% endif %}

{% if page.secondary %}
  <div id="secondary-links" class="clear-block">
    {{ page.secondary }}
  </div>
{% endif %}

<div id="header-wrapper" class="clear-block">
  <!-- If Site Logo Enabled -->
  {% if logo %}
    <div id="site-logo">
      <a href="{{ front_page }}" title="{{ 'Home'|t }}">
        <img src="{{ logo }}" alt="{{ 'Home'|t }}"/>
      </a>
      {% if site_slogan %}
        <div class='site-slogan'>
          <h2>{{ site_slogan }}</h2>
        </div>
      {% endif %}
    </div>
  {% endif %}

  <!-- If Site Name Enabled -->
  {% if site_name %}
    <div id="site-name">
      <a href="{{ front_page }}" title="{{ 'Home'|t }}">{{ site_name }}</a>
      {% if site_slogan %}
        <div class='site-slogan'>
          <h2>{{ site_slogan }}</h2>
        </div>
      {% endif %}
    </div>
  {% endif %}

  <!-- Top Right Banner -->
  {% if page.top_banner %}
    <div id="topbanner">
      {{ page.top_banner }}
    </div>
  {% endif %}

  {% if use_default_banner and top_banner_default %}
    <div id="topbanner-default">
      <img src="{{ top_banner_default }}"/>
    </div>
  {% endif %}
</div><!-- End of header-wrapper -->


<!-- Top User Regions -->
{% if page.user1 or page.user2 or page.user3 %}
  <div id="topboxes" class="clear-block">
    {% if page.user1 %}
      <div class="userbox {{ top_boxes_width }}">
        <div class="userbox-inner">
          {{ page.user1 }}
        </div>
      </div>
    {% endif %}
    {% if page.user2 %}
      <div class="userbox {{ top_boxes_width }}">
        <div class="userbox-inner">
          {{ page.user2 }}
        </div>
      </div>
    {% endif %}
    {% if page.user3 %}
      <div class="userbox {{ top_boxes_width }}">
        <div class="userbox-inner">
          {{ page.user3 }}
        </div>
      </div>
    {% endif %}
  </div>
{% endif %}<!-- End of Top User Regions -->

<!-- Main Layout Div & Conditional Statement -->
<div id="middle-wrapper" class="clear-block">
  {{ layout_content }}
  {#{% include "@pluraltheme/portal.html.twig" %}#}
</div>

<!-- Bottom User Regions -->
{% if page.user4 or page.user5 or page.user6 %}
  <div id="bottomboxes" class="clear-block">
    {% if page.user4 %}
      <div class="userbox-bottom {{ bottom_boxes_width }}">
        <div class="userbox-bottom-inner">
          {{ page.user4 }}
        </div>
      </div>
    {% endif %}
    {% if page.user5 %}
      <div class="userbox-bottom {{ bottom_boxes_width }}">
        <div class="userbox-bottom-inner">
          {{ page.user5 }}
        </div>
      </div>
    {% endif %}
    {% if page.user6 %}
      <div class="userbox-bottom {{ bottom_boxes_width }}">
        <div class="userbox-bottom-inner">
          {{ page.user6 }}
        </div>
      </div>
    {% endif %}
  </div><!-- End of Bottom User Regions -->
{% endif %}

<!-- The All Knowing All Seeing Footer Block -->
{% if page.footer %}
  <footer id="footer" class="clear-block">
    {{ page.footer }}
  </footer>
{% endif %}

<div style="text-align: center; padding: 5px;">
  <a href="http://www.greenlandtechnology.com/"> A GreenLand Technologies SMS Company </a>
</div>
